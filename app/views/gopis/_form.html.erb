<%= form_for(@gopi) do |f| %>
  <% if @gopi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gopi.errors.count, "error") %> prohibited this gopi from being saved:</h2>

      <ul>
      <% @gopi.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
